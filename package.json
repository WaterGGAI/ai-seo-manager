{
  "name": "ai-seo-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source AI SEO management platform",
  "scripts": {
    "dev": "npm --prefix worker run dev",
    "deploy": "npm --prefix worker run deploy",
    "check": "npm --prefix worker run check"
  }
}
